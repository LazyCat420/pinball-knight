# Rust
target/
**/target/
**/*.rs.bk

# Node & Frontend
node_modules/
**/node_modules/
dist/
**/dist/

# Docs build output (mdbook serve/build)
/docs/book

# Editor/OS noise
.DS_Store
*.swp

# pk-check output
/.checks

# Local git worktrees & builds
/.worktrees/
/build-windows/

# Deploy output (deploy.sh writes the test shard logs here)
**/.deploy-logs/

# Death-lab / sandbox death-trace dumps — regenerated by the tests that write
# them (engine/render/sandbox-*-death-trace.test.ts) and by scripts/death-lab.mjs.
# Evidence, not source: committing it leaves the working tree dirty, and the
# ship guard refuses a working tree it cannot identify.
ThreeJS/src/game/pinball-knight/tools/sprite-forge/work/
ThreeJS/.death-lab/
ThreeJS/.pipeline-forensics/
ThreeJS/.death-audit/

# death-swarm and boss-death-lab capture artefacts (screenshots, contact sheets, report.json)
ThreeJS/.death-swarm*/
ThreeJS/.boss-death-lab*/
